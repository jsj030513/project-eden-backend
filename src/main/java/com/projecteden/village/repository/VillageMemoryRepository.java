package com.projecteden.village.repository;import java.util.*;import org.springframework.data.jpa.repository.JpaRepository;import com.projecteden.village.domain.*;public interface VillageMemoryRepository extends JpaRepository<VillageMemory,Long>{Optional<VillageMemory> findByCharacterIdAndCategory(Long characterId,VillageCategory category);List<VillageMemory> findByCharacterId(Long characterId);}
