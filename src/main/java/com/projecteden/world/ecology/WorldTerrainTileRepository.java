package com.projecteden.world.ecology; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository; public interface WorldTerrainTileRepository extends JpaRepository<WorldTerrainTile,Long>{ List<WorldTerrainTile> findByCharacterIdOrderByYAscXAsc(Long characterId); Optional<WorldTerrainTile> findByCharacterIdAndXAndY(Long characterId,int x,int y); boolean existsByCharacterId(Long characterId); }
