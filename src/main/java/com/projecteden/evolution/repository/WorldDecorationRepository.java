package com.projecteden.evolution.repository;import java.util.List;import org.springframework.data.jpa.repository.JpaRepository;import com.projecteden.evolution.domain.*;public interface WorldDecorationRepository extends JpaRepository<WorldDecoration,Long>{List<WorldDecoration> findByCharacterId(Long characterId);boolean existsByCharacterIdAndDecorationType(Long characterId,DecorationType decorationType);}
